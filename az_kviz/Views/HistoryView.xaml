<UserControl x:Class="az_kviz.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#f0f0f0">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="HISTORIE HER" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20" 
                   Foreground="#333"/>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding HistoryEntries}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  CanUserAddRows="False"
                  Background="White" 
                  AlternatingRowBackground="#f9f9f9"
                  BorderBrush="#ccc" 
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Datum" Binding="{Binding [Datum]}" Width="1.5*"/>
                <DataGridTextColumn Header="Hráč 1" Binding="{Binding [Hrac1]}" Width="*"/>
                <DataGridTextColumn Header="Hráč 2" Binding="{Binding [Hrac2]}" Width="*"/>
                <DataGridTextColumn Header="Vítěz" Binding="{Binding [Vitez]}" Width="*"/>
                <DataGridTextColumn Header="Režim" Binding="{Binding [Rezim]}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" 
                Content="Zpět do hlavního menu" 
                Command="{Binding BackToMenuCommand}"
                Height="40" 
                Width="200" 
                Margin="0,20,0,0"
                FontSize="16"
                Background="#444" 
                Foreground="White"
                Cursor="Hand"/>
    </Grid>
</UserControl>